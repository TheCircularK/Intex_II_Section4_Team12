@model string

@if (!String.IsNullOrEmpty(Model))
{
    var statusMessageClass = Model.StartsWith("Error") ? "danger" : "success";
    <div style="text-align:center" class="alert alert-@statusMessageClass alert-dismissible" role="alert">
        @Model
    </div>
}
